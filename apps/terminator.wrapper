#!/bin/bash
HOURS=$(date +%H)
if [ $HOURS -lt ${DAYSTART:-8} ] || [ $HOURS -ge ${NIGHTSTART:-22} ]; then
    echo je noc
    exec terminator -p dark --working-directory=`xcwd` "$@"
fi
echo je den
exec terminator --working-directory=`xcwd`  "$@"
